import { SharedHeader } from "@/components/ui/SharedHeader";
export default function Notification() {
  // Runs only once when the component mounts

  return <SharedHeader title="Notification"></SharedHeader>;
}
